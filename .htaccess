<IfModule mod_rewrite.c>
    RewriteEngine on
    # Redirect empty requests to public/
    RewriteRule ^$ public/ [L]
    
    # Redirect all other requests to public/ if not already there
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
